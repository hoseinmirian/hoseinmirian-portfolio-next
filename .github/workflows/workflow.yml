name: CI - Test & Quality Checks

on:
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: 🧪 Unit & Integration Tests + Coverage
    runs-on: ubuntu-latest
    outputs:
      preview_url: ${{ steps.vercel.outputs.url }}
    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 🧩 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Run ESLint
        run: npm run lint

      - name: 🧪 Run unit tests with coverage
        run: npm run coverage

      - name: ☁️ Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          flags: unittests

      - name: ⏳ Wait for Vercel preview deployment
        uses: patrickedqvist/wait-for-vercel-preview@v1.3.2
        id: vercel
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          max_timeout: 600

  e2e:
    name: 🌐 Cypress E2E Tests (Preview)
    runs-on: ubuntu-latest
    needs: test
    if: ${{ needs.test.outputs.preview_url != '' }}
    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🧩 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧪 Run Cypress E2E on preview
        env:
          CYPRESS_BASE_URL: ${{ needs.test.outputs.preview_url }}
        run: npx cypress run --e2e